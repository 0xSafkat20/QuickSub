import SiteLink from '../ui/SiteLink';
export default function PageNavigation({current,accountHref='/account'}:{current:'checkout'|'account'|'tracking'|'cart';accountHref?:string}) {
 return <header className="bg-white border-b border-brand-100"><div className="max-w-5xl mx-auto p-4 sm:p-5 flex flex-wrap items-center justify-between gap-3"><SiteLink href="/" className="font-heading text-xl font-extrabold text-brand-700">QuickSub</SiteLink><nav aria-label="Customer navigation" className="flex flex-wrap gap-1 text-sm font-semibold">{[{label:'Browse products',href:'/#products',key:'store'},{label:'My cart',href:'/cart',key:'cart'},{label:'My account',href:accountHref,key:'account'},{label:'Track order',href:'/track',key:'tracking'}].map(link=><SiteLink key={link.key} href={link.href} aria-current={current===link.key?'page':undefined} className={`px-3 py-3 rounded-xl ${current===link.key?'bg-brand-50 text-brand-800':'text-brand-600 hover:bg-brand-50'}`}>{link.label}</SiteLink>)}</nav></div></header>;
}
