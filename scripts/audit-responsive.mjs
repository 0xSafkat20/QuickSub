import puppeteer from 'puppeteer';import {startTestServer} from './admin-test-server.mjs';
const env=await startTestServer({demoCheckout:true});let browser;
try{browser=await puppeteer.launch({executablePath:'C:/Program Files/Google/Chrome/Application/chrome.exe',headless:true});const page=await browser.newPage();await page.setRequestInterception(true);page.on('request',r=>r.url().startsWith(env.base)||r.url().startsWith('data:')?r.continue():r.abort());await page.evaluateOnNewDocument(()=>localStorage.setItem('quicksub-cookie-consent','accepted'));
for(const width of [320,390,768,1024,1440]){await page.setViewport({width,height:800});for(const route of ['/','/checkout?product=3','/account','/admin','/?payment=return']){await page.goto(env.base+route,{waitUntil:'networkidle0'});if(route==='/admin'){await page.evaluate(async()=>fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json','x-quicksub-client':'web'},body:JSON.stringify({email:'owner@example.test',password:'test-password'})}));await page.reload({waitUntil:'networkidle0'});}const result=await page.evaluate(()=>({width:innerWidth,scroll:document.documentElement.scrollWidth,overflow:[...document.querySelectorAll('body *')].filter(e=>{const r=e.getBoundingClientRect();return r.width&&r.right>innerWidth+2&&getComputedStyle(e).position!=='fixed'&&!e.closest('table');}).slice(0,8).map(e=>({tag:e.tagName,cls:e.className,text:e.textContent.slice(0,40)}))}));console.log(JSON.stringify({route,...result}));}}
}finally{await browser?.close();await env.close();}
